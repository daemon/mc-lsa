apply plugin: 'idea'
apply plugin: 'java'

version = '0.0.1'

repositories {
    mavenCentral()
    flatDir {
       dirs 'libs'
    }
    maven {
        url "https://oss.sonatype.org/content/repositories/snapshots"
    }
}

sourceSets {
    main {
        java {
            srcDirs = ['src/main']
        }
    }
}

configurations {
    provided
    compile.extendsFrom provided
}

task bukkitPlugin(type: Jar) {
    baseName = 'mclsa-bukkit'
    manifest {
        attributes 'Implementation-Title': 'mc-lsa',
                   'Implementation-Version': version
    }
    from (sourceSets.main.output) {
        include "net/rocketeer/mclsa/**"
    }
    from ('src/main/resources') {
        include "plugin.yml"
    }
    from {
        (configurations.compile - configurations.provided).collect { it.isDirectory() ? it : zipTree(it) }
    }
}

dependencies {
    provided name: 'spigot-api-1.9.4'
    provided name: 'worldedit-bukkit-6.1.3'
    compile group: 'org.apache.commons', name: 'commons-math3', version: '3.0'
    provided group: 'com.google.code.gson', name: 'gson', version: '2.7'
}

buildDir = 'bin'
